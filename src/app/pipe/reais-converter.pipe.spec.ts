import { ReaisConverterPipe } from './reais-converter.pipe';

describe('ReaisConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new ReaisConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
